<Window x:Class="WpfDataGrid.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfDataGrid.Views"
        mc:Ignorable="d"
        Title="ShellView" Height="484.8" Width="700" WindowStartupLocation="CenterScreen" >

    <Grid>
        <Border Padding="10">
            <StackPanel>
                <TextBlock Grid.Row="1" Margin="10 0 10 10" FontSize="28" Text="Fund Basic Info Navigator"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="3*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Margin="10" FontSize="15" Text="Bond Code:"/>
                    <TextBox Text="{Binding BondListString}" Grid.Column="1" Margin="10" FontSize="15" FontWeight="Bold" />
                    <Button x:Name = "SearchButtonClick" Grid.Column="2"  FontSize="15" Margin="10">Search</Button>
                </Grid>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="4*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Margin="10" FontSize="15" Text="Execute Status:"/>
                    <TextBlock Text="{Binding ExcuteStatus}" Grid.Column="1" Margin="10" FontSize="15" />
                </Grid>

                <Grid>
                    <ScrollViewer  VerticalScrollBarVisibility="Auto" Height="Auto">
                        <DataGrid Height="300" x:Name="Fund" AlternatingRowBackground="Gold" CanUserAddRows="False" AutoGenerateColumns="False"  >
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="FundCode" Binding="{Binding Path=FundCode}"/>
                                <DataGridTextColumn Header="Name" Binding="{Binding Path=Name}"/>
                                <DataGridTextColumn Header="NetValueDate" Binding="{Binding Path=JZRQ}"/>
                                <DataGridTextColumn Header="UnitNetValue" Binding="{Binding Path=DWJZ}"/>
                                <DataGridTextColumn Header="EstimateValue" Binding="{Binding Path=GSZ}"/>
                                <DataGridTextColumn Header="EstimateValueRate" Binding="{Binding Path=GSZZL}"/>
                                <DataGridTextColumn Header="EstimateDate" Binding="{Binding Path=GzTime}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </ScrollViewer >
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</Window>
